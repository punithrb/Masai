let tasks=["Morning walk", "finish pending assessments", "volleyball", "gym", "classes"];
tasks.splice(0,1)

tasks.unshift("Read the book", "College")

tasks[tasks.length-1]="practice coading"
console.log(tasks)
